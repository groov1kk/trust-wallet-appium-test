package com.github.groov1kk.widgets;

import org.openqa.selenium.WebElement;

/** Representation of android.widget.ImageView. */
public class Image extends CustomWidget {

  protected Image(WebElement element) {
    super(element);
  }
}
