package com.github.groov1kk.widgets;

import org.openqa.selenium.WebElement;

/** Representation of android.widget.TextView. */
public class Text extends CustomWidget {

  protected Text(WebElement element) {
    super(element);
  }
}
